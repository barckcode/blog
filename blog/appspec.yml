version: 0.0
os: linux
files:
  - source: /blog
    destination: /var/www/helmcode_com
hooks:
  ApplicationStop:
    - location: /scripts/stop_blog.sh
      timeout: 300
  AfterInstall:
    - location: /scripts/after_install_blog.sh
      timeout: 600
